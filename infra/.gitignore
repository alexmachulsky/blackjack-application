# Terraform state files — never commit
*.tfstate
*.tfstate.*
*.tfstate.backup

# Local variable files contain secrets
terraform.tfvars
*.auto.tfvars

# Terraform working directory (cache — never commit)
.terraform/

# Lock file should be committed for reproducibility (see: terraform.io/language/files/dependency-lock)
# .terraform.lock.hcl

# Crash logs
crash.log
crash.*.log

# Override files (local developer overrides)
override.tf
override.tf.json
*_override.tf
*_override.tf.json
